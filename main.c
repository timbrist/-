#include<stdio.h>
#include<stdlib.h>
#include"Controller.h"
//#include"Model.h"

int main()
{
    printf("Hello world\n");
    MainWindow();    
    //int filelines = GetFileLines("StudentInfo.txt");
    //printf("filelines:%d\n",filelines);
    //student *stu = (student *)malloc(sizeof(student) *filelines);
    //GetStudentLine(stu, filelines);

    //printf("elective:%d", stu[1].elective);

    //free(stu);

    return 0;
}
